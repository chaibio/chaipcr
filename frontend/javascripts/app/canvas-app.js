angular.module("canvasApp", [])
.config([function() {
  // work which needs to be performed on module loading
}]).run([function() {
  // work which should be performed when the injector is done loading all modules.
}]).
constant('ChaiBioTech', "ChaiBioTech");
